name: release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build-binaries:
    name: ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux-amd64
            os: ubuntu-latest
            goos: linux
            goarch: amd64
          - target: linux-arm64
            os: ubuntu-latest
            goos: linux
            goarch: arm64
          # Darwin builds run on macos-latest so we can ad-hoc codesign the
          # output natively. Ad-hoc signing is enough to survive the macOS
          # Sequoia/Tahoe provenance gate when spawned by signed hosts
          # (Claude Desktop, etc).
          - target: darwin-amd64
            os: macos-latest
            goos: darwin
            goarch: amd64
          - target: darwin-arm64
            os: macos-latest
            goos: darwin
            goarch: arm64
          - target: windows-amd64
            os: ubuntu-latest
            goos: windows
            goarch: amd64
            ext: .exe
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
          cache-dependency-path: server/go.sum
      - name: Build ${{ matrix.target }}
        working-directory: server
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: |
          version="${GITHUB_REF_NAME#v}"
          mkdir -p ../dist
          go build -trimpath -ldflags="-s -w -X main.Version=${version}" \
            -o "../dist/ltm-${{ matrix.target }}${{ matrix.ext }}" .
      - name: Ad-hoc codesign (darwin only)
        if: matrix.goos == 'darwin'
        run: |
          codesign --sign - --force --timestamp=none \
            "dist/ltm-${{ matrix.target }}${{ matrix.ext }}"
          codesign -d -v "dist/ltm-${{ matrix.target }}${{ matrix.ext }}"
      - name: Archive
        run: |
          cd dist
          if [ "${{ matrix.goos }}" = "windows" ]; then
            zip "ltm-${{ matrix.target }}.zip" "ltm-${{ matrix.target }}${{ matrix.ext }}"
          else
            tar -czf "ltm-${{ matrix.target }}.tar.gz" "ltm-${{ matrix.target }}${{ matrix.ext }}"
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: ltm-${{ matrix.target }}
          path: |
            dist/ltm-${{ matrix.target }}.tar.gz
            dist/ltm-${{ matrix.target }}.zip
          if-no-files-found: ignore

  build-extension:
    name: vsix
    # macos-latest so the cross-compiled darwin binaries inside the bundle
    # can be ad-hoc codesigned by extension/scripts/build-bins.sh.
    runs-on: macos-latest
    needs: build-binaries
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
          cache-dependency-path: server/go.sum
      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: npm
          cache-dependency-path: extension/package-lock.json
      - name: Cross-compile bundled binaries (with darwin codesign)
        working-directory: extension
        run: ./scripts/build-bins.sh
      - name: Install extension deps
        working-directory: extension
        run: npm ci
      - name: Package vsix
        working-directory: extension
        run: |
          npx --yes @vscode/vsce package --out "l0-memory-${GITHUB_REF_NAME#v}.vsix"
      - uses: actions/upload-artifact@v4
        with:
          name: vsix
          path: extension/*.vsix

  release:
    name: github release
    runs-on: ubuntu-latest
    needs: [build-binaries, build-extension]
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Flatten artifacts
        run: |
          mkdir -p release
          find artifacts -type f \( -name '*.tar.gz' -o -name '*.zip' -o -name '*.vsix' \) -exec cp {} release/ \;
          ls -la release/
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
          generate_release_notes: true
